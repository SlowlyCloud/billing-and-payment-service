exports = class Item {
  constructor(id, name, desc, amount, price, info) {
    this.id = id
    this.name = name
    this.desc = desc
    this.amount = amount
    this.price = price
    this.info = info
  }
}
